    <div class="bg-zinc-800 p-4 rounded-md mb-2 flex justify-between items-start">
        <div>
            <p class="text-gray-300 font-semibold"><%= new Date(entry.date).toDateString() %></p>
            <p class="text-white mt-1"><%= entry.content %></p>
        </div>
        <div class="flex flex-col space-y-1">
            <form action="/entries/<%= entry._id %>?_method=PATCH" method="POST">
                <input type="text" name="content" placeholder="Edit content" class="px-2 py-1 rounded-md mb-1">
                <button type="submit" class="bg-blue-500 px-3 py-1 rounded-md text-white">Edit</button>
            </form>
            <form action="/entries/<%= entry._id %>?_method=DELETE" method="POST">
                <button type="submit" class="bg-red-500 px-3 py-1 rounded-md text-white">Delete</button>
            </form>
        </div>
    </div>    

<!-- Removed full HTML scaffolding so it acts as a true partial and only renders the card markup. -->

